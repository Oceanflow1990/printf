#include "main.h"

/**
 * _strlen - Return length of a string
 * @str: string pointer
 *
 * Return: 1
 */

int _strlen(char *str)
{
	int i;

	for (i = 0; i < str[i]; i++)
		;
	return (i);

}

/**
 * _strlenc - Return length of a string
 * @str: string pointer
 *
 * Return: 1
 */

int _strlenc(const char *str)
{
	int i;

	for (i = 0; i < str[i]; i++)
		;
	return (i);
}
